<table class="table table-bordered">


<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<!-- <table> -->
  <thead>
    <tr class="success table table-hove">

      <th class="success">Title</th>
      <th class="success">User</th>
      <th class="success">Content</th>
      <th class="success">Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>

        <td class="success"><%= post.title %></td>
        <td class="success"><%= post.user_id %></td>
        <td class="success"><%= post.content %></td>
        <td class="success"><%= image_tag(post.image, :alt => "user image")%> </td>
        <td class="success"><%= link_to 'Show', post, class:"btn btn-info"  %></td>
        <td class="success"><%= link_to 'Edit', edit_post_path(post), class:"btn btn-info"  %></td>
        <td class="success"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-info" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</table>
<%= link_to 'New Post', new_post_path, class:"btn btn-info" %>
